/**
 * ENUM para setear los nombre de las variables de configuración. Así éstas cambien más adelante, los nombres seteados no cambiarán.
 */
export enum CONFIGURATION_ENUM {
  PORT = 'PORT',
  HOST = 'HOST',
  PG_PORT = 'PG_PORT',
  PG_HOST = 'PG_HOST',
  PG_USERNAME = 'PG_USERNAME',
  PG_PASSWORD = 'PG_PASSWORD',
  PG_DATABASE = 'PG_DATABASE',
  JWT_SECRET = 'JWT_SECRET',
  GOOGLE_ID = 'GOOGLE_ID',
  GOOGLE_SECRET = 'GOOGLE_SECRET',
}
